# Fail-closed production identity gate. Replace both UNCONFIGURED values in one
# reviewed change only after the dedicated hosted Supabase project has passed
# staging restore, Auth/RLS, backup, and security verification. The anon key is
# public in the APK; its SHA-256 is pinned here to prevent release-time drift.
SUPABASE_URL=UNCONFIGURED
SUPABASE_ANON_KEY_SHA256=UNCONFIGURED
PRIVACY_POLICY_STATUS=UNAPPROVED
PRIVACY_NOTICE_VERSION=3
